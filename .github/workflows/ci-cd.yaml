name: CI/CD

on:
  pull_request:
  push:
    branches:
      - master
    tags:
      - 'v*'
  workflow_dispatch:

concurrency:
  group: ci-cd-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Required PR status check: ruff + svelte-check + Prettier + full pytest suite + frontend production build.
  validate:
    name: Validate application
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          enable-cache: true
          cache-dependency-glob: packages/backend/uv.lock

      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0

      - name: Install backend dependencies
        working-directory: packages/backend
        run: uv sync --frozen

      - name: Lint backend
        working-directory: packages/backend
        run: env -u VIRTUAL_ENV uv run ruff check app tests

      - name: Check backend formatting
        working-directory: packages/backend
        run: env -u VIRTUAL_ENV uv run ruff format --check app tests

      # In-memory SQLite — no services needed.
      - name: Test backend
        working-directory: packages/backend
        run: env -u VIRTUAL_ENV uv run python -m pytest -q

      - name: Install frontend dependencies
        working-directory: packages/frontend
        run: bun install --frozen-lockfile

      - name: Typecheck frontend
        working-directory: packages/frontend
        run: bun run check

      - name: Check frontend formatting
        working-directory: packages/frontend
        run: bun run format:check

      - name: Production frontend build
        working-directory: packages/frontend
        run: bun run build

  image:
    name: Build container image
    needs: validate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4

      # Same-repo PRs may push dev-* tags; fork PRs only build (no registry write).
      - name: Log in to GitHub Container Registry
        if: >-
          github.event_name != 'pull_request' ||
          github.event.pull_request.head.repo.full_name == github.repository
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate image metadata
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            # Production floating tags — only on merge/push to master (never PRs).
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/master' && github.event_name != 'pull_request' }}
            type=raw,value=master,enable=${{ github.ref == 'refs/heads/master' && github.event_name != 'pull_request' }}
            # Immutable production commit tag.
            type=sha,prefix=sha-,format=short,enable=${{ github.event_name != 'pull_request' }}
            # Release tags (v1.2.3 → 1.2.3, 1.2).
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            # PR / pre-merge images use a dev- prefix and never share production tags.
            type=ref,event=pr,prefix=dev-
            type=sha,prefix=dev-sha-,format=short,enable=${{ github.event_name == 'pull_request' }}

      - name: Build and publish image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7
        with:
          context: .
          file: docker/Dockerfile
          # Deployment stack pins linux/amd64 (see deployments compose files / just image).
          platforms: linux/amd64
          # Push production tags from master/tags; push dev-* tags from same-repo PRs only.
          push: >-
            ${{
              github.event_name != 'pull_request' ||
              github.event.pull_request.head.repo.full_name == github.repository
            }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          annotations: ${{ steps.meta.outputs.annotations }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: mode=max
          sbom: true
